@model Cineplex.ViewModels.ShowVm

@{
    ViewData["Title"] = "Create";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Create</h1>

<h4>Show</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="ShowName" class="control-label"></label>
                <input asp-for="ShowName" class="form-control" />
                <span asp-validation-for="ShowName" class="text-danger"></span>
            </div>
            @* <div class="form-group">
                <label asp-for="MovieId" class="control-label"></label>
                <select asp-for="MovieId" class ="form-control" asp-items="ViewBag.MovieId"></select>
            </div> *@
            <div class="form-group">
                <label asp-for="MovieId">Movie</label>
                @Html.DropDownListFor(
                         model => model.MovieId,
                Model.Movies,
                "Select a Movie",
                new { @class = "form-control" }
                )
            </div>
            @* <div class="form-group">
                <label asp-for="HallId" class="control-label"></label>
                <select asp-for="HallId" class ="form-control" asp-items="ViewBag.HallId"></select>
            </div> *@
            <div class="form-group">
                <label asp-for="HallId">Hall</label>
                @Html.DropDownListFor(
                model => model.HallId,
                Model.Halls,
                "Select a Hall",
                new { @class = "form-control" }
                )
            </div>
            <div class="form-group">
                <label asp-for="ShowStart" class="control-label"></label>
                <input asp-for="ShowStart" class="form-control" />
                <span asp-validation-for="ShowStart" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="ShowEnd" class="control-label"></label>
                <input asp-for="ShowEnd" class="form-control" />
                <span asp-validation-for="ShowEnd" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
